enum StudentStutus { present, absent, vacation }

enum Gender { male, female }

enum EnumStage { primary, intermediate, preparatory }

enum FileType { image, pdf, url }

enum AccountType { teacher, student, parent }
